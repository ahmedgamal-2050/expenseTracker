import { CategoryOptionPipe } from './category-option.pipe';

describe('CategoryOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
